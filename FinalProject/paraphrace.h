#ifndef PARAPHRACE_MAIN //Ignored in Makefile
#define PARAPHRACE_MAIN

char** paraBuild(char* file, int count, int depth); //You can directly put in the file name here
char* arrayToStr(char** array);                     //Or just input it in the terminal

#endif
